import {
  CHANNEL_KINDS,
  PROTOCOLS,
  PROTOCOL_IDS,
  protocolById,
  type Protocol,
} from '@refract/contracts'

export { PROTOCOLS, PROTOCOL_IDS, protocolById }
export type ProtocolId = Protocol

export const KINDS = CHANNEL_KINDS
